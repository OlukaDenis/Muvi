<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.premar.muvi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.premar.muvi.activity.MovieDetailActivity"
            android:theme="@style/WithNoActionBar">

        </activity>
        <activity
            android:name="com.premar.muvi.activity.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.premar.muvi.activity.TodoActivity" />
        <activity android:name="com.premar.muvi.activity.MainActivity" />
        <activity android:name="com.premar.muvi.activity.WelcomeActivity">
        </activity>
        <activity android:name="com.premar.muvi.activity.NewsActivity" />
    </application>

</manifest>